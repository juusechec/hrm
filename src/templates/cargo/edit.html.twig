{% extends 'hrm.html.twig' %}

{% block title %}Editar Cargo{% endblock %}

{% block body %}
<div class="card">
    <div class="header">
        <h2>Editar Cargo</h2>
    </div>
    <div class="body">
        {{ include('cargo/_form.html.twig', {'button_label': 'Actualizar'}) }}
    /div>
</div>

<div class="row clearfix">
    <div class="col-lg-3 col-md-3 col-sm-6 col-xs-12">
        <a href="{{ path('cargo_index') }}"class="btn btn-block btn-lg btn-success waves-effect">Volver a la lista</a>
    </div>
</div>

{{ include('cargo/_delete_form.html.twig') }}
<br>
{% endblock %}
