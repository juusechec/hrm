{% extends 'hrm.html.twig' %}

{% block title %}Concepto Examen Medico{% endblock %}

{% block body %}
<div class="row clearfix">
    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
        <div class="card">
            <div class="header">
                <h2>Concepto Examen Medico</h2>
            </div>
            <div class="body table-responsive">
                <table class="table table-striped">
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ concepto_examen_medico.id }}</td>
                    </tr>
                    <tr>
                        <th>Nombre</th>
                        <td>{{ concepto_examen_medico.nombre }}</td>
                    </tr>
                    <tr>
                        <th>Descripción</th>
                        <td>{{ concepto_examen_medico.descripcion }}</td>
                    </tr>
                    <tr>
                        <th>Abreviación</th>
                        <td>{{ concepto_examen_medico.abreviacion }}</td>
                    </tr>
                    <tr>
                        <th>Orden</th>
                        <td>{{ concepto_examen_medico.orden }}</td>
                    </tr>
                    <tr>
                        <th>Activo</th>
                        <td>{{ concepto_examen_medico.activo ? 'Sí' : 'No' }}</td>
                    </tr>
                </tbody>
            </table>
            </div>
        </div>
    </div>
</div>
<div class="row clearfix">
    <div class="col-lg-3 col-md-3 col-sm-6 col-xs-12">
        <a href="{{ path('concepto_examen_medico_index') }}"class="btn btn-block btn-lg btn-success waves-effect">Volver a la Lista</a>
    </div>
    <div class="col-lg-3 col-md-3 col-sm-6 col-xs-12">
        <a href="{{ path('concepto_examen_medico_edit', {'id': concepto_examen_medico.id}) }}"class="btn btn-block btn-lg btn-primary waves-effect">Editar</a>
    </div>
</div>

    {{ include('concepto_examen_medico/_delete_form.html.twig') }}
    </br>
{% endblock %}
