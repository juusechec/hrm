{% extends 'hrm.html.twig' %}

{% block title %}ConceptoExamenMedico{% endblock %}

{% block body %}
    <h1>ConceptoExamenMedico</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ concepto_examen_medico.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ concepto_examen_medico.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ concepto_examen_medico.descripcion }}</td>
            </tr>
            <tr>
                <th>Abreviacion</th>
                <td>{{ concepto_examen_medico.abreviacion }}</td>
            </tr>
            <tr>
                <th>Orden</th>
                <td>{{ concepto_examen_medico.orden }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{{ concepto_examen_medico.activo ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('concepto_examen_medico_index') }}">back to list</a>

    <a href="{{ path('concepto_examen_medico_edit', {'id': concepto_examen_medico.id}) }}">edit</a>

    {{ include('concepto_examen_medico/_delete_form.html.twig') }}
{% endblock %}
