{% extends 'hrm.html.twig' %}

{% block title %}Titulo Academico{% endblock %}

{% block body %}
    <h1>Titulo Academico</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ titulo_academico.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ titulo_academico.nombre }}</td>
            </tr>
            <tr>
                <th>CodigoSnies</th>
                <td>{{ titulo_academico.codigoSnies }}</td>
            </tr>
            <tr>
                <th>ModalidadAcademica</th>
                <td>{{ titulo_academico.modalidadAcademica }}</td>
            </tr>
            <tr>
                <th>Descripción</th>
                <td>{{ titulo_academico.descripcion }}</td>
            </tr>
            <tr>
                <th>Abreviación</th>
                <td>{{ titulo_academico.abreviacion }}</td>
            </tr>
            <tr>
                <th>Orden</th>
                <td>{{ titulo_academico.orden }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{{ titulo_academico.activo ? 'Sí' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('titulo_academico_index') }}">Volver a la Lista</a>

    <a href="{{ path('titulo_academico_edit', {'id': titulo_academico.id}) }}">Editar</a>

    {{ include('titulo_academico/_delete_form.html.twig') }}
{% endblock %}
