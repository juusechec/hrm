<form method="post" action="{{ path('elemento_dotacion_delete', {'id': elemento_dotacion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ elemento_dotacion.id) }}">
    <button class="btn">Delete</button>
</form>
