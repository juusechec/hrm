{% extends 'hrm.html.twig' %}

{% block title %}ElementoDotacion{% endblock %}

{% block body %}
    <h1>ElementoDotacion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ elemento_dotacion.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ elemento_dotacion.nombre }}</td>
            </tr>
            <tr>
                <th>Descripción</th>
                <td>{{ elemento_dotacion.descripcion }}</td>
            </tr>
            <tr>
                <th>Abreviación</th>
                <td>{{ elemento_dotacion.abreviacion }}</td>
            </tr>
            <tr>
                <th>Orden</th>
                <td>{{ elemento_dotacion.orden }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{{ elemento_dotacion.activo ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('elemento_dotacion_index') }}">Volver a la Lista</a>

    <a href="{{ path('elemento_dotacion_edit', {'id': elemento_dotacion.id}) }}">edit</a>

    {{ include('elemento_dotacion/_delete_form.html.twig') }}
{% endblock %}
