{% extends 'base.html.twig' %}

{% block title %}TipoVivienda{% endblock %}

{% block body %}
    <h1>TipoVivienda</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipo_vivienda.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ tipo_vivienda.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ tipo_vivienda.descripcion }}</td>
            </tr>
            <tr>
                <th>Abreviacion</th>
                <td>{{ tipo_vivienda.abreviacion }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{{ tipo_vivienda.activo ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Orden</th>
                <td>{{ tipo_vivienda.orden }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tipo_vivienda_index') }}">back to list</a>

    <a href="{{ path('tipo_vivienda_edit', {'id': tipo_vivienda.id}) }}">edit</a>

    {{ include('tipo_vivienda/_delete_form.html.twig') }}
{% endblock %}
