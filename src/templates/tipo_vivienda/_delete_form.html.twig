<form method="post" action="{{ path('tipo_vivienda_delete', {'id': tipo_vivienda.id}) }}" onsubmit="return confirm('¿Está seguro que desea eliminar este ítem?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tipo_vivienda.id) }}">
    <button class="btn">Eliminar</button>
</form>
