<form method="post" action="{{ path('educacion_superior_delete', {'id': educacion_superior.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ educacion_superior.id) }}">
    <button class="btn">Delete</button>
</form>
