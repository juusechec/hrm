<form method="post" action="{{ path('educacion_superior_delete', {'id': educacion_superior.id}) }}" onsubmit="return confirm('¿Está seguro que desea eliminar este ítem?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ educacion_superior.id) }}">
    <button class="btn">Borrar</button>
</form>
