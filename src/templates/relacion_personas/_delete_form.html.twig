<form method="post" action="{{ path('relacion_personas_delete', {'id': relacion_persona.id}) }}" onsubmit="return confirm('¿Está seguro que desea eliminar este ítem?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ relacion_persona.id) }}">
    <button class="btn">Borrar</button>
</form>
