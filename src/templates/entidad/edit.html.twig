{% extends 'hrm.html.twig' %}
{% form_theme form 'form/fields.html.twig' %}

{% block title %}Editar Entidad{% endblock %}

{% block body %}
<div class="card">
    <div class="header">
        <h2>Editar Entidad</h2>
    </div>
    <div class="body">
        {{ include('entidad/_form.html.twig', {'button_label': 'Actualizar'}) }}
    </div>
</div>

<div class="row clearfix">
    <div class="col-lg-3 col-md-3 col-sm-6 col-xs-12">
        <a href="{{ path('entidad_index') }}" class="btn btn-block btn-lg btn-success waves-effect">Volver a la lista</a>
    </div>
</div>
{{ include('entidad/_delete_form.html.twig') }}
<br>
{% endblock %}
