<form method="post" action="{{ path('prorroga_contrato_delete', {'id': prorroga_contrato.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prorroga_contrato.id) }}">
    <button class="btn">Delete</button>
</form>
