{% extends 'base.html.twig' %}

{% block title %}ProcesoGestionIntegral index{% endblock %}

{% block body %}
    <h1>ProcesoGestionIntegral index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Descripcion</th>
                <th>Abreviacion</th>
                <th>Orden</th>
                <th>Activo</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for proceso_gestion_integral in proceso_gestion_integrals %}
            <tr>
                <td>{{ proceso_gestion_integral.id }}</td>
                <td>{{ proceso_gestion_integral.nombre }}</td>
                <td>{{ proceso_gestion_integral.descripcion }}</td>
                <td>{{ proceso_gestion_integral.abreviacion }}</td>
                <td>{{ proceso_gestion_integral.orden }}</td>
                <td>{{ proceso_gestion_integral.activo ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('proceso_gestion_integral_show', {'id': proceso_gestion_integral.id}) }}">show</a>
                    <a href="{{ path('proceso_gestion_integral_edit', {'id': proceso_gestion_integral.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('proceso_gestion_integral_new') }}">Create new</a>
{% endblock %}
