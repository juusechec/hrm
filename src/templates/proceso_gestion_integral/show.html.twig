{% extends 'base.html.twig' %}

{% block title %}ProcesoGestionIntegral{% endblock %}

{% block body %}
    <h1>ProcesoGestionIntegral</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ proceso_gestion_integral.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ proceso_gestion_integral.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ proceso_gestion_integral.descripcion }}</td>
            </tr>
            <tr>
                <th>Abreviacion</th>
                <td>{{ proceso_gestion_integral.abreviacion }}</td>
            </tr>
            <tr>
                <th>Orden</th>
                <td>{{ proceso_gestion_integral.orden }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{{ proceso_gestion_integral.activo ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('proceso_gestion_integral_index') }}">back to list</a>

    <a href="{{ path('proceso_gestion_integral_edit', {'id': proceso_gestion_integral.id}) }}">edit</a>

    {{ include('proceso_gestion_integral/_delete_form.html.twig') }}
{% endblock %}
