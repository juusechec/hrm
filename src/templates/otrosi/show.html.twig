{% extends 'hrm.html.twig' %}

{% block title %}Otrosi{% endblock %}

{% block body %}
    <h1>Otrosi</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ otrosi.id }}</td>
            </tr>
            <tr>
                <th>FechaInicio</th>
                <td>{{ otrosi.fechaInicio ? otrosi.fechaInicio|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('otrosi_index') }}">back to list</a>

    <a href="{{ path('otrosi_edit', {'id': otrosi.id}) }}">edit</a>

    {{ include('otrosi/_delete_form.html.twig') }}
{% endblock %}
