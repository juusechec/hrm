{% extends 'base.html.twig' %}

{% block title %}AuxilioContrato index{% endblock %}

{% block body %}
    <h1>AuxilioContrato index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Fecha</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for auxilio_contrato in auxilio_contratos %}
            <tr>
                <td>{{ auxilio_contrato.id }}</td>
                <td>{{ auxilio_contrato.fecha ? auxilio_contrato.fecha|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('auxilio_contrato_show', {'id': auxilio_contrato.id}) }}">show</a>
                    <a href="{{ path('auxilio_contrato_edit', {'id': auxilio_contrato.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('auxilio_contrato_new') }}">Create new</a>
{% endblock %}
