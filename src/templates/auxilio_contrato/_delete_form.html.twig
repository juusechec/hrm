<form method="post" action="{{ path('auxilio_contrato_delete', {'id': auxilio_contrato.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ auxilio_contrato.id) }}">
    <div class="row clearfix">
        <div class="col-lg-3 col-lg-offset-9 col-md-3 col-md-offset-9 col-sm-6 col-sm-offset-6 col-xs-12">
            <button class="btn btn-block btn-lg btn-danger waves-effect">Eliminar</button>
        </div>
    </div>
</form>
