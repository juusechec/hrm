{% extends 'base.html.twig' %}

{% block title %}AuxilioContrato{% endblock %}

{% block body %}
    <h1>AuxilioContrato</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ auxilio_contrato.id }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ auxilio_contrato.fecha ? auxilio_contrato.fecha|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('auxilio_contrato_index') }}">back to list</a>

    <a href="{{ path('auxilio_contrato_edit', {'id': auxilio_contrato.id}) }}">edit</a>

    {{ include('auxilio_contrato/_delete_form.html.twig') }}
{% endblock %}
