{% extends 'hrm.html.twig' %}

{% block title %}Genero{% endblock %}

{% block body %}
<div class="row clearfix">
    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
        <div class="card">
            <div class="header">
                <h2>Genero</h2>
            </div>
            <div class="body table-responsive">
                <table class="table table-striped">
                    <tbody>
                        <tr>
                            <th>Id</th>
                            <td>{{ genero.id }}</td>
                        </tr>
                        <tr>
                            <th>Nombre</th>
                            <td>{{ genero.nombre }}</td>
                        </tr>
                        <tr>
                            <th>Descripción</th>
                            <td>{{ genero.descripcion }}</td>
                        </tr>
                        <tr>
                            <th>Abreviación</th>
                            <td>{{ genero.abreviacion }}</td>
                        </tr>
                        <tr>
                            <th>Orden</th>
                            <td>{{ genero.orden }}</td>
                        </tr>
                        <tr>
                            <th>Activo</th>
                            <td>{{ genero.activo ? 'Sí' : 'No' }}</td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
<div class="row clearfix">
    <div class="col-lg-3 col-md-3 col-sm-6 col-xs-12">
        <a href="{{ path('genero_index') }}" class="btn btn-block btn-lg btn-success waves-effect">Volver a la lista</a>
    </div>
    <div class="col-lg-3 col-md-3 col-sm-6 col-xs-12">
        <a href="{{ path('genero_edit', {'id': genero.id}) }}" class="btn btn-block btn-lg btn-primary waves-effect">Editar</a>
    </div>
</div>

{{ include('genero/_delete_form.html.twig') }}
<br>
{% endblock %}
