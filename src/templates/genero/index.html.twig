{% extends 'hrm.html.twig' %}

{% block title %}Genero index{% endblock %}

{% block body %}
    <h1>Genero index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Descripcion</th>
                <th>Abreviacion</th>
                <th>Orden</th>
                <th>Activo</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for genero in generos %}
            <tr>
                <td>{{ genero.id }}</td>
                <td>{{ genero.nombre }}</td>
                <td>{{ genero.descripcion }}</td>
                <td>{{ genero.abreviacion }}</td>
                <td>{{ genero.orden }}</td>
                <td>{{ genero.activo ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('genero_show', {'id': genero.id}) }}">show</a>
                    <a href="{{ path('genero_edit', {'id': genero.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">!Ups! no encontramos nada</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('genero_new') }}">Create new</a>
{% endblock %}
