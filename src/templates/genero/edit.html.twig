{% extends 'hrm.html.twig' %}

{% block title %}Edit Genero{% endblock %}

{% block body %}
    <h1>Edit Genero</h1>

    {{ include('genero/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('genero_index') }}">Volver a la Lista</a>

    {{ include('genero/_delete_form.html.twig') }}
{% endblock %}
