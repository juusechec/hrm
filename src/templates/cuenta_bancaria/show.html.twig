{% extends 'hrm.html.twig' %}

{% block title %}Cuenta Bancaria{% endblock %}

{% block body %}
    <h1>Cuenta Bancaria</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cuenta_bancarium.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ cuenta_bancarium.numero }}</td>
            </tr>
            <tr>
                <th>Tipo</th>
                <td>{{ cuenta_bancarium.tipo }}</td>
            </tr>
            <tr>
                <th>Principal</th>
                <td>{{ cuenta_bancarium.principal ? 'Sí' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cuenta_bancaria_index') }}">Volver a la lista</a>

    <a href="{{ path('cuenta_bancaria_edit', {'id': cuenta_bancarium.id}) }}">Editar</a>

    {{ include('cuenta_bancaria/_delete_form.html.twig') }}
{% endblock %}
