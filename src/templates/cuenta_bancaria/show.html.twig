{% extends 'hrm.html.twig' %}

{% block title %}Cuenta Bancaria{% endblock %}

{% block body %}
<div class="row clearfix">
    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
        <div class="card">
            <div class="header">
                <h2>Cuenta Bancaria</h2>
            </div>
            <div class="body table-responsive">
                <table class="table table-striped">
                    <tbody>
                        <tr>
                            <th>Id</th>
                            <td>{{ cuenta_bancarium.id }}</td>
                        </tr>
                        <tr>
                            <th>Numero</th>
                            <td>{{ cuenta_bancarium.numero }}</td>
                        </tr>
                        <tr>
                            <th>Tipo</th>
                            <td>{{ cuenta_bancarium.tipo }}</td>
                        </tr>
                        <tr>
                            <th>Principal</th>
                            <td>{{ cuenta_bancarium.principal ? 'Sí' : 'No' }}</td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
<div class="row clearfix">
    <div class="col-lg-3 col-md-3 col-sm-6 col-xs-12">
        <a href="{{ path('cuenta_bancaria_index') }}" class="btn btn-block btn-lg btn-success waves-effect">Volver a la lista</a>
    </div>
    <div class="col-lg-3 col-md-3 col-sm-6 col-xs-12">
        <a href="{{ path('cuenta_bancaria_edit', {'id': cuenta_bancarium.id}) }}" class="btn btn-block btn-lg btn-primary waves-effect">Editar</a>
    </div>
</div>

{{ include('cuenta_bancaria/_delete_form.html.twig') }}
<br>
{% endblock %}
