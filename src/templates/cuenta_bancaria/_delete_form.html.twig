<form method="post" action="{{ path('cuenta_bancaria_delete', {'id': cuenta_bancarium.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cuenta_bancarium.id) }}">
    <button class="btn">Delete</button>
</form>
